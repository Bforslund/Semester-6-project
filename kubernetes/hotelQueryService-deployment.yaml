apiVersion: apps/v1
kind: Deployment
metadata:
  name: hotelqueryservice
  labels:
    app: hotelqueryservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hotelqueryservice
  template:
    metadata:
      labels:
        app: hotelqueryservice
    spec:
      containers:
        - name: hotelqueryservice
          image: bforslund/semester-6-hotelqueryservice:122730
          imagePullPolicy: Always
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: DbConnectionString
              value: Server=hoteldatabase.mysql.database.azure.com;UserID = adminbea;Password=Password1;Database=hoteldatabase;
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
            limits:
              cpu: 200m
